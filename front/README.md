# Frontend (Next.js)

## Run locally
```bash
cd front
npm i
copy .env.local.example .env.local
npm run dev
```

- App: http://localhost:3000

## Note
- API base는 `.env.local`의 `NEXT_PUBLIC_API_BASE`로 설정합니다.
